{
  "name": "rcsd-mcp-server",
  "version": "1.0.0",
  "description": "RCSD Open Data MCP Server — query Redwood City School District data",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node test.mjs",
    "test:local": "node test.mjs http://localhost:8787"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "agents": "^0.20.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260510.1",
    "wrangler": "^4.100.0"
  },
  "//overrides": "ws: miniflare (via wrangler) pins ws@8.18.0 exactly, vulnerable to GHSA-58qx-3vcg-4xpx (fixed in 8.20.1). Remove once wrangler ships a miniflare with ws >= 8.20.1. esbuild: wrangler pins esbuild@0.27.x, vulnerable to GHSA-gv7w-rqvm-qjhr + GHSA-g7r4-m6w7-qqqr (fixed in 0.28.1). Remove once wrangler ships esbuild >= 0.28.1.",
  "overrides": {
    "ws": "^8.20.1",
    "esbuild": "^0.28.1"
  }
}
