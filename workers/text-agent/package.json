{
  "name": "rcsd-text-agent",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "node test.mjs",
    "spike": "node spike-mcp-connector.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260714.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.119.0"
  },
  "//overrides": "undici: miniflare (via wrangler) pins undici@7.28.0 exactly, below the patched 7.29.0. Dependabot cannot open a PR for it — the only unforced path to 7.29.0 downgrades wrangler — so this override is the fix. Remove once wrangler ships a miniflare with undici >= 7.29.0.",
  "overrides": {
    "undici": "^7.29.0"
  }
}
