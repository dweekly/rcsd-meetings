{
  "name": "rcsd-text-agent",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "node test.mjs",
    "spike": "node spike-mcp-connector.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260714.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.118.0"
  },
  "//overrides": "undici: wrangler pins undici@7.28.0 exactly via miniflare, so Dependabot cannot open a PR at all — its only path to the patched 7.29.0 would downgrade wrangler 4.118.0 -> 4.35.0, so it errors out every cycle and the alerts never move. Remove once wrangler ships a miniflare with undici >= 7.29.0.",
  "overrides": {
    "undici": "^7.29.0"
  }
}
